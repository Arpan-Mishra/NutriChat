/// Feature flags — all default to `false`. Flip to `true` to enable experimental features.
enum FeatureFlags {
    static let aiQuickLog = false
    static let newBarcodeScanner = false
    static let voiceLogging = false
    static let healthKitSync = false
}
